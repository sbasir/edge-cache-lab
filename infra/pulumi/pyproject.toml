[project]
name = "edge-cache-lab"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
    "mypy>=2.3.0",
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.19.0",
]

[tool.mypy]
exclude = '(^|/)(\.venv|venv)/'
ignore_missing_imports = true
