# golangci-lint v2 config. Run: golangci-lint run  /  golangci-lint fmt
version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unconvert
    - unused

formatters:
  enable:
    - gofmt
    - goimports
